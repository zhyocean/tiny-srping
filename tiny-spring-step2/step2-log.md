# tiny-spring

### 1.tiny-spring-step2
a、使用BeanFactory接口，含有获得Bean以及注册Bean方法

b、抽象类AbstractBeanFactory实现BeanFactory接口，并将Bean的初始化放入BeanFactory

c、Bean的初始化<br>
    （1）定义一个Bean，通过反射创建该Bean的class<br>
    （2）在注册Bean时初始化Bean信息，通过class反射创建一个Bean


